macros:
  - name: cents_to_dollars
    description: "Convert an amount in cents to dollars, with an optional number of decimal places."
    arguments:
      - name: column_name
        description: "The name of the column containing the amount in cents."
      - name: decimals
        description: "The number of decimal places to round to (default is 2)."